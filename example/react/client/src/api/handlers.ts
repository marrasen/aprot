// Code generated by aprot. DO NOT EDIT.
// Handler: Handlers
import { useCallback } from 'react';
import { ApiClient, useQuery, usePushEvent } from './client';
import type {
    RequestOptions,
    UseQueryOptions,
    UseQueryResult,
    PushHandler,
    UsePushResult,
} from './client';

export const TaskStatus = {
    Created: "created",
    Running: "running",
    Completed: "completed",
    Failed: "failed",
} as const;
export type TaskStatusType = typeof TaskStatus[keyof typeof TaskStatus];

export interface CreateUserResponse {
    id: string;
    name: string;
    email: string;
}

export interface GetDashboardResponse {
    usersByRole: Record<string, User[]>;
    featuredUsers: User[];
    tagsById: Record<number, Tag>;
}

export interface GetTaskResponse {
    id: string;
    name: string;
    status: TaskStatusType;
}

export interface GetUserResponse {
    id: string;
    name: string;
    email: string;
}

export interface ListUsersResponse {
    users: User[];
}

export interface ProcessBatchResponse {
    processed: number;
    results: string[];
}

export interface StartSharedWorkResponse {
    completed: number;
    totalSteps: number;
    totalDuration: number;
    results: StepResult[];
}

export interface StepResult {
    step: string;
    duration: number;
    hash?: string;
    error?: string;
}

export interface SystemNotificationEvent {
    message: string;
    level: string;
}

export interface Tag {
    id: string;
    name: string;
    color: string;
}

export interface User {
    id: string;
    name: string;
    email: string;
}

export interface UserCreatedEvent {
    id: string;
    name: string;
    email: string;
}

export interface UserUpdatedEvent {
    id: string;
    name: string;
    email: string;
}


export function createUser(client: ApiClient, name: string, email: string, options?: RequestOptions): Promise<CreateUserResponse> {
    return client.request<CreateUserResponse>('Handlers.CreateUser', [name, email], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
createUser.method = 'Handlers.CreateUser' as const;

export function subscribeCreateUser(client: ApiClient, name: string, email: string, callback: (data: CreateUserResponse) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<CreateUserResponse>('Handlers.CreateUser', [name, email], callback, onError);
}


export function getDashboard(client: ApiClient, options?: RequestOptions): Promise<GetDashboardResponse> {
    return client.request<GetDashboardResponse>('Handlers.GetDashboard', [], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
getDashboard.method = 'Handlers.GetDashboard' as const;

export function subscribeGetDashboard(client: ApiClient, callback: (data: GetDashboardResponse) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<GetDashboardResponse>('Handlers.GetDashboard', [], callback, onError);
}


export function getTask(client: ApiClient, id: string, options?: RequestOptions): Promise<GetTaskResponse> {
    return client.request<GetTaskResponse>('Handlers.GetTask', [id], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
getTask.method = 'Handlers.GetTask' as const;

export function subscribeGetTask(client: ApiClient, id: string, callback: (data: GetTaskResponse) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<GetTaskResponse>('Handlers.GetTask', [id], callback, onError);
}


export function getUser(client: ApiClient, id: string, options?: RequestOptions): Promise<GetUserResponse> {
    return client.request<GetUserResponse>('Handlers.GetUser', [id], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
getUser.method = 'Handlers.GetUser' as const;

export function subscribeGetUser(client: ApiClient, id: string, callback: (data: GetUserResponse) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<GetUserResponse>('Handlers.GetUser', [id], callback, onError);
}


export function listUsers(client: ApiClient, options?: RequestOptions): Promise<ListUsersResponse> {
    return client.request<ListUsersResponse>('Handlers.ListUsers', [], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
listUsers.method = 'Handlers.ListUsers' as const;

export function subscribeListUsers(client: ApiClient, callback: (data: ListUsersResponse) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<ListUsersResponse>('Handlers.ListUsers', [], callback, onError);
}


export function processBatch(client: ApiClient, items: string[], delay: number, options?: RequestOptions): Promise<ProcessBatchResponse> {
    return client.request<ProcessBatchResponse>('Handlers.ProcessBatch', [items, delay], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
processBatch.method = 'Handlers.ProcessBatch' as const;

export function subscribeProcessBatch(client: ApiClient, items: string[], delay: number, callback: (data: ProcessBatchResponse) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<ProcessBatchResponse>('Handlers.ProcessBatch', [items, delay], callback, onError);
}


export function sendNotification(client: ApiClient, message: string, level: string, options?: RequestOptions): Promise<SystemNotificationEvent> {
    return client.request<SystemNotificationEvent>('Handlers.SendNotification', [message, level], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
sendNotification.method = 'Handlers.SendNotification' as const;

export function subscribeSendNotification(client: ApiClient, message: string, level: string, callback: (data: SystemNotificationEvent) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<SystemNotificationEvent>('Handlers.SendNotification', [message, level], callback, onError);
}


export function startSharedWork(client: ApiClient, title: string, steps: string[], delay: number, options?: RequestOptions): Promise<StartSharedWorkResponse> {
    return client.request<StartSharedWorkResponse>('Handlers.StartSharedWork', [title, steps, delay], options);
}
// Wire-method tag consumed by useQuerySuspense to key the promise cache and
// open the matching server subscription. Stable identifier across builds
// (unaffected by minification, unlike Function.name).
startSharedWork.method = 'Handlers.StartSharedWork' as const;

export function subscribeStartSharedWork(client: ApiClient, title: string, steps: string[], delay: number, callback: (data: StartSharedWorkResponse) => void, onError?: (error: Error) => void): () => void {
    return client.subscribe<StartSharedWorkResponse>('Handlers.StartSharedWork', [title, steps, delay], callback, onError);
}

export function onUserCreatedEvent(client: ApiClient, handler: PushHandler<UserCreatedEvent>): () => void {
    return client.onPush<UserCreatedEvent>('UserCreatedEvent', handler);
}

export function onUserUpdatedEvent(client: ApiClient, handler: PushHandler<UserUpdatedEvent>): () => void {
    return client.onPush<UserUpdatedEvent>('UserUpdatedEvent', handler);
}

export function onSystemNotificationEvent(client: ApiClient, handler: PushHandler<SystemNotificationEvent>): () => void {
    return client.onPush<SystemNotificationEvent>('SystemNotificationEvent', handler);
}

// React Hooks for Handlers

/**
 * Subscribes to `Handlers.CreateUser` with the given parameters and re-renders
 * automatically when the server triggers a refresh. When the parameters
 * change, the previous subscription is canceled and a new one starts.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useCreateUser(name: string, email: string, options?: UseQueryOptions): UseQueryResult<CreateUserResponse> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal, name: string, email: string) => createUser(client, name, email, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, params: [name, email], _subscribe: { method: 'Handlers.CreateUser', params: [name, email] } });
}

/**
 * Subscribes to `Handlers.GetDashboard` and re-renders automatically when the
 * server triggers a refresh. The subscription is cleaned up on unmount.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useGetDashboard(options?: UseQueryOptions): UseQueryResult<GetDashboardResponse> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal) => getDashboard(client, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, _subscribe: { method: 'Handlers.GetDashboard', params: [] } });
}

/**
 * Subscribes to `Handlers.GetTask` with the given parameters and re-renders
 * automatically when the server triggers a refresh. When the parameters
 * change, the previous subscription is canceled and a new one starts.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useGetTask(id: string, options?: UseQueryOptions): UseQueryResult<GetTaskResponse> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal, id: string) => getTask(client, id, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, params: [id], _subscribe: { method: 'Handlers.GetTask', params: [id] } });
}

/**
 * Subscribes to `Handlers.GetUser` with the given parameters and re-renders
 * automatically when the server triggers a refresh. When the parameters
 * change, the previous subscription is canceled and a new one starts.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useGetUser(id: string, options?: UseQueryOptions): UseQueryResult<GetUserResponse> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal, id: string) => getUser(client, id, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, params: [id], _subscribe: { method: 'Handlers.GetUser', params: [id] } });
}

/**
 * Subscribes to `Handlers.ListUsers` and re-renders automatically when the
 * server triggers a refresh. The subscription is cleaned up on unmount.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useListUsers(options?: UseQueryOptions): UseQueryResult<ListUsersResponse> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal) => listUsers(client, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, _subscribe: { method: 'Handlers.ListUsers', params: [] } });
}

/**
 * Subscribes to `Handlers.ProcessBatch` with the given parameters and re-renders
 * automatically when the server triggers a refresh. When the parameters
 * change, the previous subscription is canceled and a new one starts.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useProcessBatch(items: string[], delay: number, options?: UseQueryOptions): UseQueryResult<ProcessBatchResponse> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal, items: string[], delay: number) => processBatch(client, items, delay, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, params: [items, delay], _subscribe: { method: 'Handlers.ProcessBatch', params: [items, delay] } });
}

/**
 * Subscribes to `Handlers.SendNotification` with the given parameters and re-renders
 * automatically when the server triggers a refresh. When the parameters
 * change, the previous subscription is canceled and a new one starts.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useSendNotification(message: string, level: string, options?: UseQueryOptions): UseQueryResult<SystemNotificationEvent> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal, message: string, level: string) => sendNotification(client, message, level, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, params: [message, level], _subscribe: { method: 'Handlers.SendNotification', params: [message, level] } });
}

/**
 * Subscribes to `Handlers.StartSharedWork` with the given parameters and re-renders
 * automatically when the server triggers a refresh. When the parameters
 * change, the previous subscription is canceled and a new one starts.
 * See {@link UseQueryResult} for return value details — including the
 * query-scoped `mutate(action)` helper for refetch-after-mutation flows.
 */
export function useStartSharedWork(title: string, steps: string[], delay: number, options?: UseQueryOptions): UseQueryResult<StartSharedWorkResponse> {
    const wrappedFn = useCallback(
        (client: ApiClient, signal: AbortSignal, title: string, steps: string[], delay: number) => startSharedWork(client, title, steps, delay, { signal }),
        [],
    );
    return useQuery(wrappedFn, { ...options, params: [title, steps, delay], _subscribe: { method: 'Handlers.StartSharedWork', params: [title, steps, delay] } });
}

export function useUserCreatedEvent(options?: { maxEvents?: number }): UsePushResult<UserCreatedEvent> {
    return usePushEvent(onUserCreatedEvent, options);
}

export function useUserUpdatedEvent(options?: { maxEvents?: number }): UsePushResult<UserUpdatedEvent> {
    return usePushEvent(onUserUpdatedEvent, options);
}

export function useSystemNotificationEvent(options?: { maxEvents?: number }): UsePushResult<SystemNotificationEvent> {
    return usePushEvent(onSystemNotificationEvent, options);
}
